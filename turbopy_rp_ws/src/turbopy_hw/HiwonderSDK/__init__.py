"""Vendored HiwonderSDK for TurboPi ROS2 drivers.

This makes the HiwonderSDK directory a Python package so it
is installed alongside turbopy_hw and importable as
`HiwonderSDK` (e.g. `import HiwonderSDK.Board`).
"""
